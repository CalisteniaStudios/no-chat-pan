{
  "name": "no-chat-pan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A system-agnostic Foundry VTT module that disables chat camera panning.",
  "scripts": {
    "test": "node --test",
    "check": "node tools/check.mjs",
    "build": "node tools/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
